name: iOS CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build App
        run: |
          xcodebuild clean build \
          -project PlayCam.xcodeproj \
          -scheme PlayCam \
          -destination 'platform=iOS Simulator,name=iPhone 15'

